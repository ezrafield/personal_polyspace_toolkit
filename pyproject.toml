[project]
name = "agent-native-template"
version = "0.1.0"
description = "Sample project structure for agent-native coding workflows"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

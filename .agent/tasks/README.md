# Task Logs

Use this folder for task-local notes when work has more than one step or needs handoff.

Recommended filename format:

```text
YYYY-MM-DD-short-task-name.md
```

Each task log should capture the goal, docs read, files inspected, commands run, verification, and follow-up risk. Start from `docs/agent/TASK_LOG_TEMPLATE.md` when useful.
